package farm.files;

import farm.core.farmgrid.Grid;
import java.io.IOException;

public class FileSaver {

    /**
     * Constructor for the FileSaver class.
     */
    public FileSaver() {

    }

    /**
     * Saves the contents of the provided grid to a file with specified name.
     * @param filename the path of the save file to create
     * @param grid the grid instance to save to a file
     * @throws IOException iff an error occurred when trying to write the file.
     */
    public void save(String filename, Grid grid) throws IOException {

    }
}
